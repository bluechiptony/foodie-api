export const checkIfTokenIsStillValid = (token: string): boolean => {
  return true;
};

export const checkIfTokenIsForAdminAccount = (token: string): boolean => {
  return true;
};

export const checkIfTokenIsForUserAccount = (token: string): boolean => {
  return true;
};
